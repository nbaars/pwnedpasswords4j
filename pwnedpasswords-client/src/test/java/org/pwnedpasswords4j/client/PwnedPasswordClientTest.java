package org.pwnedpasswords4j.client;

import org.junit.Test;
import org.mockito.Matchers;
import org.mockito.Mockito;

import static org.mockito.Matchers.any;
import static org.mockito.Matchers.anyString;
import static org.mockito.Mockito.when;

public class PwnedPasswordClientTest {

    @Test
    public void noResponseShouldReturnEmptyHashList() {
//        RestTemplate restTemplate = Mockito.mock(RestTemplate.class);
//        when(restTemplate.exchange(any(), any(), any(), Matchers.any(Class.class), anyString())).thenReturn(Re);
//        PwnedPasswordClient pwnedPasswordClient = new PwnedPasswordClient(restTemplate, new PwnedPasswordProperties());

        //assertThat(pwnedPasswordClient.fetchHashes(Hex.from("be")), isEm
    }

}